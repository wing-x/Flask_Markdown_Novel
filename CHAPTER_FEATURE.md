# 章追加機能の実装

新規ファイル（章）を手動で作成できる機能を追加しました。

## 追加した機能

### 1. 新規ファイル作成ボタン
- サイドバーの「📄 ファイル」セクションに「+」ボタンを追加
- プロジェクト選択時のみ有効化

### 2. ファイル作成モーダル
- ファイル名入力フィールド
- リアルタイムプレビュー（入力した名前に自動的に.md拡張子を追加）
- 章番号の自動提案機能

### 3. バックエンドAPI
- 新規エンドポイント: `POST /api/projects/<project_name>/files`
- ファイル名の重複チェック
- 章ファイル（chapterXX.md）の自動テンプレート生成

## 使い方

### 基本的な使い方
1. プロジェクトを選択
2. ファイルリストの「+」ボタンをクリック
3. ファイル名を入力（例: `chapter02`、`notes`、`ideas`）
4. 「作成」ボタンをクリック
5. 新しいファイルが自動的に開かれます

### 章ファイルの作成
- `chapter02` のように入力すると「第2章」というタイトルで作成されます
- 自動的に次の章番号が提案されます（既存の章を分析）
- 例: chapter01が存在する場合、chapter02が提案されます

### その他のファイルの作成
- `notes`、`ideas`、`research` など、任意の名前で作成可能
- 自動的に `.md` 拡張子が追加されます

## 実装の詳細

### フロントエンド
- **新規ボタン**: ファイルリストヘッダーに統合
- **モーダルUI**: プロジェクト作成と同様のデザイン
- **自動提案**: 既存の章番号を解析して次の番号を自動入力
- **リアルタイムプレビュー**: 入力中にファイル名を表示

### バックエンド
```python
@app.route('/api/projects/<project_name>/files', methods=['POST'])
def create_file(project_name):
    # ファイル名の検証
    # 重複チェック
    # 章番号の抽出とテンプレート適用
    # ファイル作成
```

### テンプレート生成ロジック
- `chapterXX.md`: 「# 第XX章」形式
- その他: 「# ファイル名」形式

## 機能の特徴

✅ **スマートな章番号提案**
既存の章を自動解析して次の番号を提案

✅ **リアルタイムプレビュー**
作成されるファイル名をその場で確認

✅ **重複防止**
同名ファイルの作成を防止

✅ **即座に編集可能**
作成後すぐにエディタで開く

✅ **柔軟な命名**
章以外のファイル（メモ、アイデアなど）も自由に作成

## 今後の拡張案

- [ ] ファイルの削除機能
- [ ] ファイルの名前変更機能
- [ ] フォルダ（セクション）による整理
- [ ] ファイルのドラッグ&ドロップによる並び替え
- [ ] テンプレート選択（キャラクター、プロット、メモなど）
- [ ] 章の複製機能
